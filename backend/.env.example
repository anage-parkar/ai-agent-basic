# LLM Provider Configuration - USE OLLAMA ONLY
LLM_PROVIDER=ollama

# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:latest

# MongoDB Configuration
MONGO_URI=mongodb+srv://Admin:Lkbd2sXR80uUBd5T@jira-task-management.croznmu.mongodb.net/analytics?retryWrites=true&w=majority
MONGO_DB=analytics

# CORS Settings
ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000

# Agent Settings
MAX_ITERATIONS=5
AGENT_TEMPERATURE=0.7

# NOTE: OpenAI and Hugging Face settings are NOT needed when using Ollama
# If you want to switch to OpenAI in the future, change LLM_PROVIDER to "openai" and add:
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4